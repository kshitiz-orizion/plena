import React from 'react'
import { Text, View } from 'react-native'

const Heading = ({title}) =>{
    return (
        <View>
            <Text>{title}</Text>
        </View>
    )
}

export default Heading